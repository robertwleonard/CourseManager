import React from "react";

const AboutPage = () => (
    <div>
        <h2>About</h2>
        <p>
            This app manages a course list with React, Redux, Router, and other libraries.
        </p>
    </div>
);

export default AboutPage;